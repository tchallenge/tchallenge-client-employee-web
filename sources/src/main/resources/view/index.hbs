<!DOCTYPE html>

{{#with setup}}
    <html>

        <head>

            <base href="{{location.base}}"/>

            <title>T-Challenge Employee Client</title>

            {{#with asset}}
                {{#each styles}}
                    <link  rel="stylesheet" href="{{uri}}"/>
                {{/each}}
                {{#each scripts}}
                    <script type="javascript" src="{{uri}}"></script>
                {{/each}}
            {{/with}}
        </head>

        <body>
            Application works: {{location.client}}
        </body>
    </html>
{{/with}}
